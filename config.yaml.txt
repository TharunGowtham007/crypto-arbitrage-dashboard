credentials:
  usernames:
    your_username:  # Change this to a simple name, like "john"
      email: your_email@example.com  # Use a fake email, like "john@test.com"
      name: Your Name  # Your real name or a fake one
      password: your_password  # A fake password, like "pass123" – it will be hashed later
    friend_username:  # Change to your friend's name, like "jane"
      email: friend_email@example.com  # Fake email
      name: Friend Name
      password: friend_password  # Fake password
cookie:
  expiry_days: 30
  key: some_random_key  # Keep this as-is; it's for security
  name: streamlit_auth
preauthorized:
  emails: []  # Leave empty for now