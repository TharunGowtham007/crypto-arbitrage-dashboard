credentials:
  usernames:
    your_username:  # e.g., "john_doe"
      email: your_email@example.com
      name: Your Name
      password: your_plain_password  # Will be hashed on first run; change to hashed value later for security
    friend_username:  # e.g., "jane_doe"
      email: friend_email@example.com
      name: Friend Name
      password: friend_plain_password
cookie:
  expiry_days: 30
  key: some_random_key  # Generate a random string for security
  name: streamlit_auth
preauthorized:
  emails: []  # Optional: List of pre-authorized emails
